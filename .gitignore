# Dependencies
#
# This is the most important rule. It prevents the massive node_modules
# directory from being checked into git. It can always be regenerated
# by running `npm install`.
node_modules/

# Environment variables
#
# Keep environment-specific files and secrets out of your repository.
.env
.env.local
.env.*.local

# Logs
#
# Log files are typically machine-specific and shouldn't be in source control.
npm-debug.log*
yarn-debug.log*

# Build output & test reports
#
# Ignore compiled files, build artifacts, and test coverage reports.
dist
coverage